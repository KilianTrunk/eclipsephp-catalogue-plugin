<div class="flex justify-center mb-4">
    <img 
        src="{{ $url }}" 
        alt="{{ $filename }}"
        class="max-w-full max-h-64 object-contain rounded-lg shadow-sm"
    />
</div>
<p class="text-center text-sm text-gray-500 dark:text-gray-400">{{ $filename }}</p>